name: Setup events-cli
description: pnpm, Node, and frozen-lockfile install for this repo
inputs:
  node-version:
    description: Node.js version
    required: true
runs:
  using: composite
  steps:
    - uses: pnpm/action-setup@v4

    - uses: actions/setup-node@v4
      with:
        node-version: ${{ inputs.node-version }}
        cache: pnpm

    - run: pnpm install --frozen-lockfile
      shell: bash
